server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #配置github
          uri: git@gitee.com:xiangmu_zongjie/shishunan-springcloud-demo-0620.git
          #配置搜索目录
          search-paths:
            - configfiles
      #配置分支
      label: test